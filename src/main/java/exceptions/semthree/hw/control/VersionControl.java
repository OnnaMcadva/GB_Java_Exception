package exceptions.semthree.hw.control;

import exceptions.semthree.hw.model.Model;

public class VersionControl implements Control {
    @Override
    public void execute(String input) {
        new Model("");
    }
}
